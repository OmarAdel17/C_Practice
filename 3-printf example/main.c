#include <stdio.h>
#include <stdlib.h>

int main()

{
    int two = 2, three = 3, five = 5;
    int result = (two+three)*5;

    printf("(%d + %d) * %d = %d", two, three, five, result);
    return 0;
}
